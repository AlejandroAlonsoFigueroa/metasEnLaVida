pipeline {
    agent any
  
    stages {
        stage('Descargar e instalar dependencias.') {
            steps {
                sh 'npm install'
            }
        }
        stage('Ejecutar pruebas unitarias.') {
            steps {
                sh 'npm run test'
            }
        }
         
    }
    
   
}
